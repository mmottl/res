(jbuild_version 1)

(executables (
  (names (buffer_ex defstrat stupid_ga weak_ex))
  (flags (
    :standard
    -w -9
    -safe-string -strict-sequence
    -principal -short-paths
  ))
  (libraries (res))
))
